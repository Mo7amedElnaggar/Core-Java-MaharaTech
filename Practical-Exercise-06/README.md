# Custom Exception Handling in Java

## Objective

Create your own exception class and write down two other classes:

- The first class will contain three methods throwing your newly created exception class.
- The second class will call the methods that throw the exception using the `try-catch-finally` block.